import styled from 'styled-components';

const Title = styled.h4({
  color: '#AAAAAA',
  fontStyle: 'normal',
  fontWeight: 'normal',
  fontSize: '2rem',
});
Title.displayName = 'h4';

export default Title;
